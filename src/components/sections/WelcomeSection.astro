---
import BaseContainer from '../base/BaseContainer.astro';
---

<section class="welcome-section">
  <BaseContainer>
    <div class="welcome-content">
      <div class="welcome-grid">
        <div class="welcome-main">
          <h2 class="welcome-title">Open Source. Open Doors.</h2>

          <p class="welcome-intro">
            HeatSync Labs is Arizona's first hackerspace, built on open source principles:
            access for everyone, knowledge shared freely, and the belief that making things
            shouldn't require permission or pedigree. We've been volunteer-run since 2009.
          </p>

          <p class="welcome-philosophy">
            We're a do-ocracy. Show up, make things, contribute back. The same ethos that
            makes open source work makes this space work: collaboration over gatekeeping,
            and no tolerance for people who break the build for everyone else.
          </p>

          <a
            href="https://wiki.heatsynclabs.org/wiki/Bylaws#Unacceptable_Behavior"
            class="welcome-link"
            target="_blank"
            rel="noopener"
          >
            Community Standards
          </a>
        </div>

        <div class="welcome-aside">
          <div class="welcome-statement">
            <p>
              We're a community space for learning, building, and making things together.
              <strong>Everyone is welcome.</strong>
            </p>

            <p>
              We actively support a diverse community. This includes, but is not limited to, LGBTQ+ makers, women and nonbinary folks,
              makers of color, people with disabilities, neurodivergent people, beginners, and first-time makers.
            </p>

            <p>
              All that's expected is curiosity, respect for others, and a willingness to build together.
            </p>
          </div>
        </div>
      </div>
    </div>
  </BaseContainer>
</section>

<style>
  .welcome-section {
    padding: var(--space-12) 0;
    background: var(--color-bg-primary);
    border-bottom: 1px solid var(--color-border);
  }

  .welcome-content {
    max-width: 1200px;
    margin: 0 auto;
  }

  .welcome-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: var(--space-12);
    align-items: start;
  }

  .welcome-main {
    max-width: 650px;
  }

  .welcome-title {
    font-size: var(--text-3xl);
    font-weight: var(--font-light);
    color: var(--color-text-primary);
    margin-bottom: var(--space-4);
    font-family: var(--font-sans);
  }

  .welcome-intro {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-6);
    font-family: var(--font-sans);
  }

  .welcome-philosophy {
    font-size: var(--text-sm);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-6);
    font-family: var(--font-sans);
  }

  .welcome-aside {
    padding-left: var(--space-8);
    border-left: 1px solid var(--color-border);
  }

  .welcome-statement p {
    font-size: var(--text-sm);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-4);
    font-family: var(--font-sans);
  }

  .welcome-statement p:first-child {
    font-size: var(--text-base);
    color: var(--color-text-primary);
  }

  .welcome-statement strong {
    font-weight: var(--font-semibold);
    color: var(--color-accent-primary);
  }

  .welcome-statement p:last-child {
    color: var(--color-text-primary);
    font-weight: var(--font-medium);
    margin-bottom: 0;
  }

  .welcome-link {
    display: inline-block;
    color: var(--color-accent-primary);
    text-decoration: none;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    letter-spacing: var(--tracking-wide);
    transition: color var(--transition-base);
    border-bottom: 1px solid transparent;
    padding-bottom: 2px;
  }

  .welcome-link:hover {
    color: var(--color-accent-secondary);
    border-bottom-color: var(--color-accent-secondary);
  }

  /* Responsive adjustments */
  @media (max-width: 1024px) {
    .welcome-grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .welcome-aside {
      padding-left: 0;
      padding-top: var(--space-6);
      border-left: none;
      border-top: 1px solid var(--color-border);
    }
  }

  @media (max-width: 768px) {
    .welcome-section {
      padding: var(--space-8) 0;
    }

    .welcome-title {
      font-size: var(--text-2xl);
    }

    .welcome-statement p {
      font-size: var(--text-xs);
    }

    .welcome-statement p:first-child {
      font-size: var(--text-sm);
    }
  }

  @media (max-width: 480px) {
    .welcome-title {
      font-size: var(--text-xl);
      margin-bottom: var(--space-3);
    }

    .welcome-intro {
      font-size: var(--text-sm);
    }

    .welcome-philosophy {
      font-size: var(--text-xs);
    }
  }
</style>